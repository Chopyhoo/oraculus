# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'oraculus' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for oraculus

    platform :ios, '6.0'
    pod "VK-ios-sdk"

  target 'oraculusTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'oraculusUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
